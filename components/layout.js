const Layout = ({children}) => {
    return (
        <div>
            
            {children}
            <footer>
                <h4>powered by caffine and boredom</h4>
            </footer>
        </div>
    )
}

export default Layout
